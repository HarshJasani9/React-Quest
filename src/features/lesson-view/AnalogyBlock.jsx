// AnalogyBlock.jsx — Phase 2
// The signature Analogy Card element (Design System §2 & §4).
// Rotation is deterministic by lesson ID (not random) so it's stable across renders.
export default function AnalogyBlock() {
  return null
}
