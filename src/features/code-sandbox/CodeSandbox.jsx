// CodeSandbox.jsx — Phase 1
// Top-level sandbox component: CodeMirror editor + preview pane.
// Pipeline: edit → debounce → Babel transform → new Function() → Error Boundary → React root.
export default function CodeSandbox() {
  return null
}
