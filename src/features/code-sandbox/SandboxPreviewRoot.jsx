// SandboxPreviewRoot.jsx — Phase 1
// Manages a dedicated React root for each checkpoint's preview pane.
// Unmounts/remounts on reset to guarantee clean state.
export default function SandboxPreviewRoot() {
  return null
}
